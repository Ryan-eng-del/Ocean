import { Empty } from 'Ocean';
import React from 'react';

const Basic = () => {
  return (
    <Empty
      description={
        <span>
          自定义 <a href="#API">Description</a>
        </span>
      }
    ></Empty>
  );
};

export default Basic;
